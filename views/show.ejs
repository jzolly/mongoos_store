<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('./partials/head.ejs') %>
		<title>Show Product</title>
	</head>
	<body>
		<nav>
			<a href="/products">Back to Product Index</a>
		</nav>
		<div class="productDisplay">
		<h1><%=product.name %></h1>
        <img class="productImg" src="<%= product.img %> " alt="<%= product.name %> " height="200px"/> <br />
		Product: <%= product.name%>
		<br />
		Description: <%= product.description %>
        <br />
        Price: $<%= product.price %> 
        <br />
        Quantity: <%= product.qty %> 
        <br />
		<form action="/cart?_method=POST" method="POST" style="display:inline;">
			<input type="text" name="productId" value="<%= product._id %>" hidden>
			<% if(product.qty > 0) { %>
				<input type="submit" value="Add to Cart">
			<% } else { %>
				<p style="color:red; display:inline;">Out of Stock</p>
			<% } %>
		</form>
        <a href="/products/<%=product._id%>/edit"><button>Edit</button></a>
	</div>
	</body>
</html>